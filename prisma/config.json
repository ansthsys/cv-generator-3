{
  "$schema": "../node_modules/prisma-zod-generator/lib/config/schema.json",
  "mode": "full",
  "pureModels": true,
  "globalExclusions": {
    "input": ["id", "cvId", "sortOrder", "createdAt", "updatedAt", "cv"]
  }
}
